/// <reference types="react" />
/// <reference types="react" />
import { ViewStyle, StyleProp } from 'react-native';
export type Props = {
    angle: number[];
    widthAndHeight: number;
    coverFill: string;
    coverRadius: number;
    doughnut: boolean;
    series: number[];
    sliceColor: string[];
    style?: StyleProp<ViewStyle>;
};
declare const Pie: ({ angle, widthAndHeight, coverFill, coverRadius, doughnut, series, sliceColor, style, }: Props) => JSX.Element;
export default Pie;
